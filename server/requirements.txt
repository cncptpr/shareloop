# Runtime dependencies for Docker builds.
# Dev dependencies (ruff, mypy, datamodel-code-generator, etc.) are in
# pyproject.toml and locked via uv.lock – not needed at runtime.
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pydantic>=2.0
pydantic-settings>=2.0
geoalchemy2>=0.14
typer>=0.9
PyYAML>=6.0
psycopg2-binary>=2.9
